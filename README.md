# Git Config CLI

A Python 3.11+ command-line tool for exploring and managing Git configuration with rich terminal output and intuitive commands.

## Getting Started

Follow the detailed setup and workflow guidance in `specs/001-git-config-cli/quickstart.md`.

## Project Status

This repository currently tracks the implementation plan generated through the Speckit workflow.
